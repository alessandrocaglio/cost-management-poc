apiVersion: v1
kind: Service
metadata:
  name: {{ include "cost-management-redux.fullname" . }}
  labels:
    {{- include "cost-management-redux.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "cost-management-redux.selectorLabels" . | nindent 4 }}
